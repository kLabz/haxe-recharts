package recharts.common;

@:coreType @:enum abstract AxisInterval from Float {
	var PreserveStart = cast 'preserveStart';
	var PreserveEnd = cast 'preserveEnd';
	var PreserveStartEnd = cast 'preserveStartEnd';
}
